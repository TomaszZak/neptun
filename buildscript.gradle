project.buildscript {
    ext {
        apply from: 'properties.gradle'
    }
    repositories {
        mavenCentral()
    }
    dependencies {
        classpath("org.springframework.boot:spring-boot-gradle-plugin:${SPRING_VER}")
    }
}